{
  "name": "virtual-folder",
  "description": "Create an in-memory virtual folder, optionally sync'd with a real folder on disk",
  "version": "0.0.2",
  "author": "Callum Locke",
  "bugs": {
    "url": "https://github.com/callumlocke/virtual-folder/issues"
  },
  "dependencies": {
    "babel": "^5.0.7",
    "bluebird": "^2.9.24",
    "buffer-equal": "0.0.1",
    "filewalker": "^0.1.2",
    "graceful-fs": "^3.0.6",
    "sane": "^1.0.2",
    "which": "^1.0.9"
  },
  "devDependencies": {
    "async": "^0.9.0",
    "coffee-script": "^1.9.1",
    "nodemon": "^1.3.7",
    "rimraf": "^2.3.2",
    "sinon": "^1.14.1",
    "source-map-support": "^0.2.10",
    "tape": "^4.0.0",
    "wrench": "^1.5.8"
  },
  "files": [
    "src",
    "lib"
  ],
  "homepage": "https://github.com/callumlocke/virtual-folder",
  "license": "MIT",
  "main": "lib/virtual-folder.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/callumlocke/virtual-folder"
  },
  "scripts": {
    "build": "babel --source-maps --out-dir lib src",
    "clean": "rimraf lib",
    "develop": "nodemon --exec 'npm run prepublish && npm test' --watch src --watch test --ext coffee,js",
    "prepublish": "npm run clean && npm run build",
    "test": "coffee ./test/acceptance.coffee"
  }
}
